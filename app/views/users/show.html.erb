<p id="notice"><%= notice %></p>

<h3>
  <%= @user.name %>
</h3>

<div class="profile">
  <p>
    <strong>Email:</strong>
    <%= @user.email %>
  </p>

  <p>
    <strong>Phone:</strong>
    <%= @user.phone %>
  </p>

  <p>
    <strong>Address:</strong>
    <%= @user.address %>
  </p>

  <p>
    <strong>License:</strong>
    <%= @user.license %>
  </p>

  <%= link_to 'Edit Your Profile', edit_user_path(@user) %>
</div>

<br>

<% unless @user.admin? %>
    <div class="panel panel-default">
      <div class="panel-heading">Active Courses</div>
      <table class="table">
        <thead>
        <tr>
          <th>Course Name</th>
          <th>Date</th>
          <th>Description</th>
        </tr>
        </thead>
        <tbody>
        <% @active_offerings.each do |offering| %>
            <tr>
              <td><%= offering.course.courseName %></td>
              <td><%= offering.courseDate %></td>
              <td><%= offering.course.courseDesc %></td>
            </tr>
        <% end %>
        </tbody>
      </table>
    </div>

    <div class="panel panel-default">
      <div class="panel-heading">Past Courses</div>
      <table class="table">
        <thead>
        <tr>
          <th>Course Name</th>
          <th>Date</th>
          <th>Description</th>
        </tr>
        </thead>
        <tbody>
        <% @past_offerings.each do |offering| %>
            <tr>
              <td><%= offering.course.courseName %></td>
              <td><%= offering.courseDate %></td>
              <td><%= offering.course.courseDesc %></td>
            </tr>
        <% end %>
        </tbody>
      </table>
    </div>

<% end %>