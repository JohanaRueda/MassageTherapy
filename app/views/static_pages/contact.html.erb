<!DOCTYPE html>
<html>
  <head></head>
<body>

<div id="Contact">
  <h2>Contactanos</h2>
  <div align="center">
  <h3>Envienos un correo electronico</h3>

  <%= form_tag '/path' do%>
  Email : </br><%= text_field_tag 'email', ''%></br>
  subject: </br><%= text_field_tag 'subject', ''%></br>
  Body:<%= text_area_tag 'body', ''%>
  <%= submit_tag "submit",:value => "submit"%>
  <%end%>

  <!--<%= form_for Contact.new, :html => {:class => 'form-horizontal' } do |f| %>
    <%= f.text_input :name, :required => true %>
    <%= f.text_input :email, :required => true %>
    <%= f.text_input :message, :as => :text, :required => true %>
    <div>
      </br>
      <%= f.button :submit, 'Send message', :class=> "btn btn-primary" %>
    </div>
  <% end %>-->
</div>
</div>

</body>
</html> 