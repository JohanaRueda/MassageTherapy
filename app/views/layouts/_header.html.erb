<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Massage BGal House", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if current_page?(root_path) %>"><%= link_to "Inicio", root_path %></li>
        <li class="<%= 'active' if current_page?(courses_path) %>"><%= link_to "Cursos", courses_path %></li>
        <% if admin? %>
            <li class="<%= 'active' if current_page?(users_path) %>"><%= link_to "Usuarios", users_path %></li>
        <% end %>
        <li class="<%= 'active' if current_page?(contact_path) %>"><%= link_to "Contactanos", contact_path %></li>
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Cuenta <b class="caret"></b>
            </a>
            <ul class="dropdown-menu" id="DropdownMenu">
              <li><%= link_to "Profile", current_user %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Cerrar sesión", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Iniciar sesión", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>